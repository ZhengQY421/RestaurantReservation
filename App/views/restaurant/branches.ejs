<!DOCTYPE html>
<html>
    <head>
        <!-- Header inclusion-->
        <%- include ../template/header.ejs %>
    </head>
    <body class="bg">
        <%- include ../template/navbar.ejs %>
        <br \>
        <h1 class="hd" style="font-size: 3em;" ><%= title %></h1>
        <br \>
        <% for(var i =0; i<data.length; i++) { %>
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title"><%= data[i].location%></h5>
                        <h6 class="card-subtitle mb-2 text-muted"><%= data[i].address%></h6>
                        <p class="card-text"><%= data[i].pnumber%></p>
                        <a href="#" class="card-link">Reserve</a>
                    </div>
                </div>
                <br \>
        <%} %>
    </body>
</html>